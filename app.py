\"\"\"NextStep AI - Intelligent Career Counselor Application\"\"\"\nimport logging\nimport os\nfrom flask import Flask, jsonify\nfrom flask_cors import CORS\nfrom dotenv import load_dotenv\n\n# Load environment variables\nload_dotenv()\n\n# Configure logging\nlogging.basicConfig(\n    level=os.getenv('LOG_LEVEL', 'INFO'),\n    format='%(asctime)s - %(name)s - %(levelname)s - %(message)s'\n)\nlogger = logging.getLogger(__name__)\n\n# Import modules\nfrom src.database.db import Database\nfrom src.rag.knowledge_base import KnowledgeBase\nfrom src.api.watsonx_client import WatsonXClient\nfrom src.agents.tools import AgentTools\nfrom src.agents.context_manager import ContextManager\nfrom src.agents.career_agent import CareerAgent\nfrom src.api.auth import AuthManager, create_auth_routes, token_required\nfrom src.api.routes import create_api_routes\n\n# Initialize Flask app\napp = Flask(__name__)\napp.config['SECRET_KEY'] = os.getenv('SECRET_KEY', 'your_secret_key_change_in_production')\n\n# Enable CORS\nCORS(app)\n\nlogger.info(\"🚀 NextStep AI Starting...\")\n\n# ============ INITIALIZE COMPONENTS ============\n\ntry:\n    # Database\n    db = Database(\n        host=os.getenv('DB_HOST', 'localhost'),\n        user=os.getenv('DB_USER', 'root'),\n        password=os.getenv('DB_PASSWORD', ''),\n        database=os.getenv('DB_NAME', 'nextstep_ai')\n    )\n    \n    if db.connect():\n        db.create_tables()\n        logger.info(\"✅ Database initialized\")\n    else:\n        logger.error(\"❌ Database connection failed\")\n        raise Exception(\"Database connection failed\")\n    \n    # Knowledge Base\n    kb = KnowledgeBase()\n    kb.load_knowledge_base()\n    logger.info(\"✅ Knowledge base loaded\")\n    \n    # Watsonx Client\n    watsonx = WatsonXClient(\n        api_key=os.getenv('WATSONX_API_KEY'),\n        project_id=os.getenv('WATSONX_PROJECT_ID'),\n        url=os.getenv('WATSONX_URL')\n    )\n    logger.info(\"✅ Watsonx client initialized\")\n    \n    # Agent Tools\n    tools = AgentTools(kb, db)\n    logger.info(\"✅ Agent tools initialized\")\n    \n    # Context Managers (per user)\n    context_managers = {}\n    \n    # Auth Manager\n    auth_manager = AuthManager(db)\n    logger.info(\"✅ Auth manager initialized\")\n    \n    logger.info(\"✅ All components initialized successfully\")\n\nexcept Exception as e:\n    logger.error(f\"❌ Initialization error: {e}\")\n    exit(1)\n\n# ============ FACTORY FUNCTIONS ============\n\ndef create_career_agent(user_id: int) -> CareerAgent:\n    \"\"\"\n    Factory function to create CareerAgent instances\n    \n    Args:\n        user_id: User ID\n        \n    Returns:\n        CareerAgent instance\n    \"\"\"\n    # Get or create context manager\n    if user_id not in context_managers:\n        context_managers[user_id] = ContextManager(user_id, tools)\n        context_managers[user_id].initialize_context()\n    \n    # Create and return agent\n    agent = CareerAgent(\n        user_id=user_id,\n        tools=tools,\n        context_manager=context_managers[user_id],\n        watsonx_client=watsonx\n    )\n    \n    return agent\n\n# ============ REGISTER BLUEPRINTS ============\n\n# Auth blueprint\nauth_bp = create_auth_routes(auth_manager)\napp.register_blueprint(auth_bp)\nlogger.info(\"✅ Auth routes registered\")\n\n# API blueprint\napi_bp = create_api_routes(create_career_agent, tools, context_managers)\napp.register_blueprint(api_bp)\nlogger.info(\"✅ API routes registered\")\n\n# ============ ERROR HANDLERS ============\n\n@app.errorhandler(404)\ndef not_found(error):\n    \"\"\"Handle 404 errors\"\"\"\n    return jsonify({\"message\": \"Resource not found\"}), 404\n\n@app.errorhandler(500)\ndef server_error(error):\n    \"\"\"Handle 500 errors\"\"\"\n    logger.error(f\"Server error: {error}\")\n    return jsonify({\"message\": \"Internal server error\"}), 500\n\n@app.errorhandler(401)\ndef unauthorized(error):\n    \"\"\"Handle 401 errors\"\"\"\n    return jsonify({\"message\": \"Unauthorized\"}), 401\n\n@app.errorhandler(403)\ndef forbidden(error):\n    \"\"\"Handle 403 errors\"\"\"\n    return jsonify({\"message\": \"Forbidden\"}), 403\n\n# ============ HEALTH CHECK ROUTES ============\n\n@app.route('/health', methods=['GET'])\ndef health():\n    \"\"\"Health check endpoint\"\"\"\n    return jsonify({\n        \"message\": \"NextStep AI is running\",\n        \"status\": \"healthy\"\n    }), 200\n\n@app.route('/api/info', methods=['GET'])\ndef info():\n    \"\"\"Get application info\"\"\"\n    return jsonify({\n        \"name\": \"NextStep AI\",\n        \"version\": \"1.0.0\",\n        \"description\": \"Intelligent Career Counselor using IBM Watsonx\",\n        \"author\": \"Saksham Sharma\",\n        \"endpoints\": {\n            \"auth\": {\n                \"register\": \"POST /api/auth/register\",\n                \"login\": \"POST /api/auth/login\",\n                \"logout\": \"POST /api/auth/logout\",\n                \"verify\": \"GET /api/auth/verify\"\n            },\n            \"chat\": {\n                \"send_message\": \"POST /api/chat\",\n                \"history\": \"GET /api/chat/history\"\n            },\n            \"profile\": {\n                \"get\": \"GET /api/profile\",\n                \"update\": \"PUT /api/profile\"\n            },\n            \"careers\": {\n                \"all\": \"GET /api/careers\",\n                \"details\": \"GET /api/careers/<career_name>\",\n                \"recommendations\": \"GET /api/careers/recommendations\"\n            },\n            \"skills\": {\n                \"all\": \"GET /api/skills\",\n                \"details\": \"GET /api/skills/<skill_name>\",\n                \"gap\": \"POST /api/skills/gap\"\n            },\n            \"learning\": {\n                \"roadmap\": \"GET /api/learning/roadmap\",\n                \"projects\": \"GET /api/learning/projects\",\n                \"certifications\": \"GET /api/learning/certifications\"\n            },\n            \"interview\": {\n                \"prep\": \"GET /api/interview/prep\"\n            },\n            \"resources\": \"GET /api/resources\"\n        }\n    }), 200\n\n# ============ MAIN ============\n\nif __name__ == '__main__':\n    port = int(os.getenv('APP_PORT', 5000))\n    debug = os.getenv('APP_DEBUG', 'False') == 'True'\n    \n    logger.info(f\"🌐 Starting NextStep AI on port {port}\")\n    logger.info(f\"🔗 API Documentation: http://localhost:{port}/api/info\")\n    logger.info(f\"💓 Health Check: http://localhost:{port}/health\")\n    \n    try:\n        app.run(host='0.0.0.0', port=port, debug=debug)\n    except Exception as e:\n        logger.error(f\"❌ Application error: {e}\")\n        db.disconnect()\n        exit(1)\n