import logging\nfrom flask import Blueprint, request, jsonify\nfrom src.api.auth import token_required\nimport json\n\nlogger = logging.getLogger(__name__)\n\napi_bp = Blueprint('api', __name__, url_prefix='/api')\n\ndef create_api_routes(career_agent_factory, tools, context_managers):\n    \"\"\"\n    Create API routes\n    \n    Args:\n        career_agent_factory: Function to create CareerAgent instances\n        tools: AgentTools instance\n        context_managers: Dictionary to store ContextManager instances\n    \"\"\"\n    \n    # ============ CHAT ROUTES ============\n    \n    @api_bp.route('/chat', methods=['POST'])\n    @token_required\n    def chat():\n        \"\"\"\n        Send message to career agent\n        \n        Request JSON:\n        {\n            \"message\": \"I'm interested in data science\"\n        }\n        \"\"\"\n        try:\n            user_id = request.user_id\n            data = request.get_json()\n            message = data.get('message', '')\n            \n            if not message:\n                return jsonify({\"message\": \"Message is required\"}), 400\n            \n            # Get or create context manager\n            if user_id not in context_managers:\n                # Create new context manager and agent\n                context_manager = context_managers.get(user_id) or context_managers.setdefault(\n                    user_id,\n                    None  # Will be created by factory\n                )\n            \n            # Get career agent\n            agent = career_agent_factory(user_id)\n            \n            # Process message (async would be better in production)\n            import asyncio\n            try:\n                loop = asyncio.get_event_loop()\n            except RuntimeError:\n                loop = asyncio.new_event_loop()\n                asyncio.set_event_loop(loop)\n            \n            response = loop.run_until_complete(agent.process_user_input(message))\n            \n            return jsonify({\n                \"message\": response.get('message'),\n                \"reasoning_steps\": response.get('reasoning_steps', []),\n                \"recommendation\": response.get('recommendation'),\n                \"type\": response.get('type')\n            }), 200\n        \n        except Exception as e:\n            logger.error(f\"❌ Chat route error: {e}\")\n            return jsonify({\"message\": f\"Chat error: {str(e)}\"}), 500\n    \n    @api_bp.route('/chat/history', methods=['GET'])\n    @token_required\n    def chat_history():\n        \"\"\"\n        Get user's chat history\n        \n        Query Parameters:\n        - limit: Number of messages to retrieve (default: 10)\n        \"\"\"\n        try:\n            user_id = request.user_id\n            limit = request.args.get('limit', 10, type=int)\n            \n            history = tools.get_chat_history(user_id, limit=limit)\n            \n            return jsonify({\n                \"message\": \"Chat history retrieved\",\n                \"history\": history,\n                \"count\": len(history)\n            }), 200\n        \n        except Exception as e:\n            logger.error(f\"❌ Chat history route error: {e}\")\n            return jsonify({\"message\": \"Server error\"}), 500\n    \n    # ============ PROFILE ROUTES ============\n    \n    @api_bp.route('/profile', methods=['GET'])\n    @token_required\n    def get_profile():\n        \"\"\"\n        Get user profile\n        \"\"\"\n        try:\n            user_id = request.user_id\n            profile = tools.get_user_profile(user_id)\n            \n            if profile:\n                return jsonify({\n                    \"message\": \"Profile retrieved\",\n                    \"profile\": profile\n                }), 200\n            else:\n                return jsonify({\n                    \"message\": \"Profile not found\",\n                    \"profile\": None\n                }), 404\n        \n        except Exception as e:\n            logger.error(f\"❌ Get profile error: {e}\")\n            return jsonify({\"message\": \"Server error\"}), 500\n    \n    @api_bp.route('/profile', methods=['PUT'])\n    @token_required\n    def update_profile():\n        \"\"\"\n        Update user profile\n        \n        Request JSON:\n        {\n            \"education\": \"Bachelor's in Computer Science\",\n            \"skills\": [\"Python\", \"SQL\", \"Machine Learning\"],\n            \"interests\": [\"AI\", \"Data Science\"],\n            \"goal\": \"Become a Data Scientist\"\n        }\n        \"\"\"\n        try:\n            user_id = request.user_id\n            data = request.get_json()\n            \n            success = tools.save_user_profile(\n                user_id,\n                data.get('education'),\n                data.get('skills', []),\n                data.get('interests', []),\n                data.get('goal')\n            )\n            \n            if success:\n                profile = tools.get_user_profile(user_id)\n                return jsonify({\n                    \"message\": \"Profile updated successfully\",\n                    \"profile\": profile\n                }), 200\n            else:\n                return jsonify({\"message\": \"Error updating profile\"}), 400\n        \n        except Exception as e:\n            logger.error(f\"❌ Update profile error: {e}\")\n            return jsonify({\"message\": \"Server error\"}), 500\n    \n    # ============ CAREER ROUTES ============\n    \n    @api_bp.route('/careers', methods=['GET'])\n    @token_required\n    def get_all_careers():\n        \"\"\"\n        Get all available careers\n        \"\"\"\n        try:\n            careers = tools.get_all_careers()\n            \n            return jsonify({\n                \"message\": \"Careers retrieved\",\n                \"careers\": careers,\n                \"count\": len(careers)\n            }), 200\n        \n        except Exception as e:\n            logger.error(f\"❌ Get careers error: {e}\")\n            return jsonify({\"message\": \"Server error\"}), 500\n    \n    @api_bp.route('/careers/<career_name>', methods=['GET'])\n    @token_required\n    def get_career_details(career_name):\n        \"\"\"\n        Get details for a specific career\n        \n        Path Parameters:\n        - career_name: Name of the career\n        \"\"\"\n        try:\n            career = tools.get_career_details(career_name)\n            \n            if career:\n                return jsonify({\n                    \"message\": \"Career details retrieved\",\n                    \"career\": career\n                }), 200\n            else:\n                return jsonify({\"message\": \"Career not found\"}), 404\n        \n        except Exception as e:\n            logger.error(f\"❌ Get career details error: {e}\")\n            return jsonify({\"message\": \"Server error\"}), 500\n    \n    @api_bp.route('/careers/recommendations', methods=['GET'])\n    @token_required\n    def get_career_recommendations():\n        \"\"\"\n        Get career recommendations based on user profile\n        \"\"\"\n        try:\n            user_id = request.user_id\n            profile = tools.get_user_profile(user_id)\n            \n            if not profile:\n                return jsonify({\n                    \"message\": \"Profile not found. Please update your profile first.\"\n                }), 400\n            \n            recommendations = tools.recommend_careers(\n                profile.get('skills', []),\n                profile.get('interests', [])\n            )\n            \n            return jsonify({\n                \"message\": \"Career recommendations retrieved\",\n                \"recommendations\": recommendations,\n                \"count\": len(recommendations)\n            }), 200\n        \n        except Exception as e:\n            logger.error(f\"❌ Get recommendations error: {e}\")\n            return jsonify({\"message\": \"Server error\"}), 500\n    \n    # ============ SKILL ROUTES ============\n    \n    @api_bp.route('/skills', methods=['GET'])\n    @token_required\n    def get_all_skills():\n        \"\"\"\n        Get all available skills\n        \"\"\"\n        try:\n            skills = tools.get_all_skills()\n            \n            return jsonify({\n                \"message\": \"Skills retrieved\",\n                \"skills\": skills,\n                \"count\": len(skills)\n            }), 200\n        \n        except Exception as e:\n            logger.error(f\"❌ Get skills error: {e}\")\n            return jsonify({\"message\": \"Server error\"}), 500\n    \n    @api_bp.route('/skills/<skill_name>', methods=['GET'])\n    @token_required\n    def get_skill_details(skill_name):\n        \"\"\"\n        Get details for a specific skill\n        \"\"\"\n        try:\n            skill = tools.get_skill_details(skill_name)\n            \n            if skill:\n                return jsonify({\n                    \"message\": \"Skill details retrieved\",\n                    \"skill\": skill\n                }), 200\n            else:\n                return jsonify({\"message\": \"Skill not found\"}), 404\n        \n        except Exception as e:\n            logger.error(f\"❌ Get skill details error: {e}\")\n            return jsonify({\"message\": \"Server error\"}), 500\n    \n    @api_bp.route('/skills/gap', methods=['POST'])\n    @token_required\n    def calculate_skill_gap():\n        \"\"\"\n        Calculate skill gap for a target career\n        \n        Request JSON:\n        {\n            \"career\": \"Data Scientist\"\n        }\n        \"\"\"\n        try:\n            user_id = request.user_id\n            data = request.get_json()\n            career = data.get('career')\n            \n            if not career:\n                return jsonify({\"message\": \"Career is required\"}), 400\n            \n            profile = tools.get_user_profile(user_id)\n            if not profile:\n                return jsonify({\"message\": \"Profile not found\"}), 400\n            \n            gap = tools.calculate_skill_gap(\n                profile.get('skills', []),\n                career\n            )\n            \n            return jsonify({\n                \"message\": \"Skill gap calculated\",\n                \"gap\": gap\n            }), 200\n        \n        except Exception as e:\n            logger.error(f\"❌ Calculate skill gap error: {e}\")\n            return jsonify({\"message\": \"Server error\"}), 500\n    \n    # ============ LEARNING ROUTES ============\n    \n    @api_bp.route('/learning/roadmap', methods=['GET'])\n    @token_required\n    def get_learning_roadmap():\n        \"\"\"\n        Get learning roadmap for a career\n        \n        Query Parameters:\n        - career: Career name\n        - level: User level (beginner/intermediate/advanced) - optional\n        \"\"\"\n        try:\n            career = request.args.get('career')\n            level = request.args.get('level', 'beginner')\n            \n            if not career:\n                return jsonify({\"message\": \"Career parameter is required\"}), 400\n            \n            roadmap = tools.generate_learning_roadmap(career, level)\n            \n            if \"error\" not in roadmap:\n                return jsonify({\n                    \"message\": \"Learning roadmap generated\",\n                    \"roadmap\": roadmap\n                }), 200\n            else:\n                return jsonify({\"message\": roadmap[\"error\"]}), 400\n        \n        except Exception as e:\n            logger.error(f\"❌ Get learning roadmap error: {e}\")\n            return jsonify({\"message\": \"Server error\"}), 500\n    \n    @api_bp.route('/learning/projects', methods=['GET'])\n    @token_required\n    def get_projects():\n        \"\"\"\n        Get recommended projects\n        \n        Query Parameters:\n        - career: Career name\n        - level: Project level (beginner/intermediate/advanced) - optional\n        \"\"\"\n        try:\n            career = request.args.get('career')\n            level = request.args.get('level', 'beginner')\n            \n            if not career:\n                return jsonify({\"message\": \"Career parameter is required\"}), 400\n            \n            projects = tools.get_projects(career, level)\n            \n            return jsonify({\n                \"message\": \"Projects retrieved\",\n                \"projects\": projects\n            }), 200\n        \n        except Exception as e:\n            logger.error(f\"❌ Get projects error: {e}\")\n            return jsonify({\"message\": \"Server error\"}), 500\n    \n    @api_bp.route('/learning/certifications', methods=['GET'])\n    @token_required\n    def get_certifications():\n        \"\"\"\n        Get recommended certifications\n        \n        Query Parameters:\n        - career: Career name\n        \"\"\"\n        try:\n            career = request.args.get('career')\n            \n            if not career:\n                return jsonify({\"message\": \"Career parameter is required\"}), 400\n            \n            certs = tools.get_certifications(career)\n            \n            return jsonify({\n                \"message\": \"Certifications retrieved\",\n                \"certifications\": certs\n            }), 200\n        \n        except Exception as e:\n            logger.error(f\"❌ Get certifications error: {e}\")\n            return jsonify({\"message\": \"Server error\"}), 500\n    \n    # ============ INTERVIEW PREP ROUTES ============\n    \n    @api_bp.route('/interview/prep', methods=['GET'])\n    @token_required\n    def get_interview_prep():\n        \"\"\"\n        Get interview preparation materials\n        \n        Query Parameters:\n        - role: Job role/career\n        \"\"\"\n        try:\n            role = request.args.get('role')\n            \n            if not role:\n                return jsonify({\"message\": \"Role parameter is required\"}), 400\n            \n            prep = tools.get_interview_prep(role)\n            \n            return jsonify({\n                \"message\": \"Interview prep materials retrieved\",\n                \"prep\": prep\n            }), 200\n        \n        except Exception as e:\n            logger.error(f\"❌ Get interview prep error: {e}\")\n            return jsonify({\"message\": \"Server error\"}), 500\n    \n    # ============ RESOURCES ROUTES ============\n    \n    @api_bp.route('/resources', methods=['GET'])\n    @token_required\n    def get_resources():\n        \"\"\"\n        Get learning resources\n        \"\"\"\n        try:\n            resources = tools.get_learning_resources()\n            \n            return jsonify({\n                \"message\": \"Resources retrieved\",\n                \"resources\": resources\n            }), 200\n        \n        except Exception as e:\n            logger.error(f\"❌ Get resources error: {e}\")\n            return jsonify({\"message\": \"Server error\"}), 500\n    \n    # ============ RECOMMENDATIONS ROUTES ============\n    \n    @api_bp.route('/recommendations', methods=['GET'])\n    @token_required\n    def get_recommendations():\n        \"\"\"\n        Get user's recommendations history\n        \n        Query Parameters:\n        - limit: Number of recommendations (default: 5)\n        \"\"\"\n        try:\n            user_id = request.user_id\n            # This would require db integration\n            # For now, returning empty\n            \n            return jsonify({\n                \"message\": \"Recommendations retrieved\",\n                \"recommendations\": []\n            }), 200\n        \n        except Exception as e:\n            logger.error(f\"❌ Get recommendations error: {e}\")\n            return jsonify({\"message\": \"Server error\"}), 500\n    \n    return api_bp\n